Differential notation d/dx requires a certified scale-normalized increment. The derivative is not a primitive; it is a shorthand for the limit of scale-normalized increments (def:VI.scale-norm-increment). No derivative may appear in a canonical argument without a certified increment underlying it. This rule prevents the import of real analysis infrastructure (limits, epsilon-delta arguments) as assumed background.
