By the branch architecture: the prime-power explicit formula is the log-derivative of Xi; the zero data is the zero set of Xi; the explicit-formula data descends via the Hadamard product representation. All three descend from O_RH = Xi by declared derivation steps, without new source objects. The SCC-MCS carrier structure (thm:SCC.MCS) provides the minimal faithful carrier for this descent.
