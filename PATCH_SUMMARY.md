# NFC Rewrite — Patch Summary

Twelve commits from baseline `d5d88c2` to final. Full per-edit detail (before/after, authority basis, gate results) is in `migrations/MIGRATIONS.md` (MIG-001…013); this is the human-readable overview of *what changed and why*.

## Scaffold built (Phases 5–6, no canon touched)
- **`scripts/extract_metadata.py`** — parses all 17 canon files into 1,790 claim records (18 fields incl. supersession, historical flag, authority tier, review-needed), 1,094 proof-citation edges, four frontier accountings, five status views. Human judgments live in `metadata/overlay.json` and survive re-extraction. The authoritative classifier is the `\status{X}` tag, not the environment name.
- **`scripts/validate.py`** — the promoted Phase-0 audit: label uniqueness (12-key benign allowlist), 0-dangling-refs invariant, 0-cycles invariant, pinned-census drift guard, `cap:` collision guard.
- **`scripts/compile_check.sh` + `Makefile`** — the edit gate as real commands (`make check`).
- **`dashboard/`** — self-contained read-only HTML dashboard (10 panels), regenerated by `make dashboard`.
- **`metadata/SCHEMA_MIGRATION_PLAN.md`** — schema documentation + the deferred `openobligation`-rename migration path.

## Scaffold repaired (MIG-001–003)
- **Canon Ledger PART 4, SM entry:** "Prospective. No SM Branch book exists yet" → current three-layer SM status; old text quoted as dated superseded description (F-1).
- **Canon Ledger PART 4, YM block:** "five obligations marked [O] in canon" → corrected to their actual `[C]` tags; old text quoted (F-2).
- **STATE_OF_CANON count table:** stale 443/209/685 → current census 461/212/748 + [B]/[R] rows, now regenerable via `make metadata` (F-3, ruled stale-not-convention).

## Canon edited (MIG-004–013) — every change status-preserving except one authorized correction
- **MIG-004 (all 17 files):** `[B]` Bridge line added to the `%%` status-legend comment (the legend had never been updated after `[B]` was introduced). Comment-only.
- **MIG-005 (BIO, CRYST, LING, SPEC, YM, SCC):** stale `%%` header status lines replaced with current one-liners pointing at each branch's status proposition; originals retained in place as marked constitutional intake.
- **MIG-006 (BIO, CRYST, LING, SPEC):** title-page "Prospective Derived Branch" → **"Constituted Derived Branch"** + "Current canonical status is governed by the Current Status Proposition in this volume." (review-approved wording).
- **MIG-007 (all 17 files):** **Current Status Capsules** — one `[R]` remark per file (`cap:*` labels), posture verbatim from the branch's own A1 proposition, pointers by `\ref`, per-branch frontier-accounting lines, constitutional-intake note. Adds no proof force and no new numbers.
- **MIG-008 (SPEC):** `prop:spec-current-status` retitled **"Initial Constitution Status (superseded)"**, reworded to constitution-time past tense, supersession pointer to `prop:SPEC-status` added. Label and `[C]` preserved (ruling B-1: it was not mis-tagged when written).
- **MIG-009/010 (SCC):** front-block status, status-vocabulary remark, `prop:scc-status` (**`[U]→[C]`**, the one authorized status change — a *weakening*), the establishes/does-not-establish boundary, and the dependency-ledger rows corrected to **"conditional CERT-CLOSE at the declared structural endpoint, conditional on the stated UCTI/depth-sum/threshold-stability and source-descent conditions."** The `thm:scc-r1` internal contradiction ("[C]" + proof + "Reserved—no theorem force") reconciled to its completed conditional proof. The consciousness/phenomenology firewall retained verbatim, unweakened, in every touched location.
- **MIG-011 (NS):** the dormant (post-`\end{document}`) future-tense "will advance" remark converted to backward-looking, citing the discharge results; the external unconditional-regularity boundary preserved.
- **MIG-012/013 (Book VII):** Branch Status Table — SM row corrected per ruling F-6 (CouplingTransfer removed from remaining/irreducible items; SM = conditionally intrinsic-structural closed, key remaining = matter/external-specification), four missing branches (CRYST, SPEC, BIO, LING) added per F-9, reconciliation note inline; the "(currently 13 items)" figure dated with a forward pointer to the obligation roster (F-8).

## Generated artifacts regenerated (never hand-edited)
All 17 PDFs rebuilt by two-pass `pdflatex` from the edited `.tex`; `metadata/*.json` and `dashboard/index.html` rebuilt from source.

## What was deliberately NOT done
- No `[O]` was discharged; no status promoted; no proof, hypothesis, or theorem statement's mathematics touched; no theorem/obligation label renamed, added (beyond `cap:`), or removed.
- `prop:SM-status` untouched (preserved baseline referenced by its reconciliation).
- The `openobligation` environment not renamed (deferred per ruling; migration path documented in the schema plan).
- CURRENT-classified honest language ("Clay gaps remain open", Book II's "remaining gap", "branch candidate" UBLT vocabulary, GR's two-status reading) left byte-identical.
